# Shared by every POC vhost: emit "Connection: upgrade" only for WebSocket
# handshakes, and "Connection: close" for ordinary requests. Hardcoding
# "upgrade" on plain HTTP proxying confuses keepalive.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
